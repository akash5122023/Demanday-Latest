@inject Serenity.ITextLocalizer Localizer
@{
    ViewData["Title"] = Localizer.Get("Db.Toolkit.TalCampaign.EntityPlural");
}

<partial name="~/Modules/Common/_GridFilterPanel.cshtml" />

<div id="GridDiv"></div>

<script type="text/javascript">
    jQuery(function () {
        var grid = new AdvanceCRM.Toolkit.TalCampaignGrid($('#GridDiv'), {}).init();
        window.attachGridFilterPanel(grid);
    });
</script>